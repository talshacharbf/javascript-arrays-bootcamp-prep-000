var chocolateBars = [
  'snickers',
  'hundred grand',
  'kit kitkat',
  'skittles',
]

function addElementToBeginningOfArray (element, array) {
  var array = [];
  return [`${element}`,...`${array}`]
}
